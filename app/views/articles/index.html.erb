<h1>Articles</h1>

 <%= render 'articles/long_featured' %>

  <div class="row mb-2">
    <%= render 'articles/medium_featured' %>
    <%= render 'articles/medium_featured' %>
  </div>
  <%= link_to "Novo Article", new_article_path%>
  <div class="row g-5">
    <div class="col-md-8">
      <h3 class="pb-4 mb-4 fst-italic border-bottom">
        From the Firehose
      </h3>
       <%= render @articles %>

      <%= render 'pagination'%>
      </div>
      <%= render 'right_side' %>
    </div>